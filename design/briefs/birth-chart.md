# Task: Ink birth chart page — implement design/ink/birth-chart.html

Read design/briefs/_shared.md first and obey every constraint there.

Build src/pages/InkBirthChartPage.js + InkBirthChartPage.css implementing design/ink/birth-chart.html with REAL data. Route params will be /dashboard/:userId/chart/:chartId (useParams; do not wire the route yourself). Data comes from the existing hook src/hooks/useChartData.js — study src/pages/ChartReaderPage.js to see every field it exposes (chart, birthChart, basicAnalysis, broadCategoryAnalyses, elements, modalities, quadrants, planetaryDominance, analysisStatus, hasAnalysis, isAnalysisComplete, handleStartAnalysis) and reuse its data-derivation patterns (flattenAnalysis from src/UI/journey/AnalysisFlow for the 360 topics, chartSceneAdapter for scene data).

Page structure per the mock:
- InkNav variant="app" (activeSegment="charts"), then chart header: "✳ {name}" + birth meta line.
- Sticky roman-numeral tab bar (i–v) switching five panels (client-side state, no scroll-jacking; scroll to top on switch like the mock):
  i. **Overview** — two-column: eyebrow + headline + basicAnalysis.overview paragraphs left; right a navy medallion card with the REAL chart: ChartScene (natal + natalAspects via adapter, topDown, disableZoom, paused when this panel is hidden) + Caveat annotation + "Drawn from your chart · {birth date}" note.
  ii. **Patterns** — pmenu sub-tabs (Shapes / Elements / Modalities / Quadrants / Planetary Influence) with "N of 5" counter; two-column viz+copy per the mock. Reuse the data flows from ChartReaderPage's lens rendering (ElementsLens/ModalitiesLens/QuadrantsLens/InfluenceLens/ShapesLens in src/UI/journey/PatternLenses.js take data + interpretation props — reuse the components if their markup can be restyled acceptably via your page CSS, otherwise build ink-styled visualizations fed by the same props; interpretations live at basicAnalysis.dominance.*.interpretation).
  iii. **Chart & Planets** — left pmenu of bodies (Ascendant, Sun…Pluto, Midheaven, Node with glyphs — canonical order used elsewhere in the app); center copy: title "{Body} in {sign} {degree}", house, interpretation paragraphs (basicAnalysis.planets[name]), aspect rows (glyph / label / orb / applying-separating — derive from birthChart.aspects exactly as src/UI/dashboard/chartTabs/PlanetsTab.js does; reuse its helpers if exportable); right a ChartScene medallion with controlled selectedBody + highlightBodies isolating the picked body (see ChartReaderPage for the controlled-selection pattern via toSceneBodyNames/fromSceneBodyName). Clicking a body in the scene selects it in the menu too.
  iv. **360 Analysis** — pmenu of life-area topics ("N of 6") from flattenAnalysis(broadCategoryAnalyses, knownNames) grouped by domain; article with title, aspect chips, sectioned paragraphs per the mock. If analysis isn't complete, show the start-analysis state (reuse the gating/CTA logic pattern from src/UI/dashboard/chartTabs/AnalysisTab.js).
  v. **Ask Stellium** — two-column: left small ChartScene medallion + "Active context" card (chart name, birth date, location); right the REAL Ask experience: reuse src/UI/gravityChat/GravityChatPanel.js — study its props (it has inline/dock rendering variants, contentType="birthchart", contentId, birthChart, suggestedQuestions) and mount it inline restyled to the mock's chat look via your page CSS; footer line "Powered by real astrology + AI · 1 credit per question". Respect the existing isAnalysisComplete gating for Ask.

Handle loading and not-my-chart guards the same way ChartReaderPage does. Do not modify ChartReaderPage or any existing page.
