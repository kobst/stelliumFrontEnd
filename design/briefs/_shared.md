# Shared constraints for all ink page tasks

- Work ONLY in /Users/edwardhan/stellium-frontend-ink (git worktree, branch feature/ink-design, React 18 CRA). Do not commit.
- FIRST read: your page's mock in design/ink/ (source of truth for layout/typography/spacing), then src/styles/ink.css and src/UI/ink/InkNav.js (the shared foundation — reuse its primitives and nav; do not fork them).
- PARALLEL-SAFETY: other agents are building sibling pages in this same worktree concurrently. You may create NEW files and edit only files no other page owns. DO NOT edit these shared files: src/styles/ink.css, src/UI/ink/*, src/App.js, src/pages/MainDashboard.js, src/index.js, public/index.html. If an ink primitive you need is missing, define it in YOUR page's own CSS file with a page-specific prefix. Do not wire routes — build your page as a self-contained default-exported component; route integration happens afterward in a separate pass. (Import InkNav into your page and render it yourself.)
- Branding: the mocks say "Iris ✳" — this app is **Stellium**. Use "Stellium ✳" and "Ask Stellium" wherever the mock says Iris/Ask Iris.
- Chart wheels: wherever the mock has an svg wheel/medallion placeholder, use our REAL 3D ephemeris instead: `import { ChartScene } from '../UI/shared/chartScene'` with adapters from src/Utilities/chartSceneAdapter (toChartScenePlacements / toChartSceneAspects / toSynastrySceneAspects / toSceneBodyNames / fromSceneBodyName). Mount it inside a rounded navy "medallion" card on the paper background (the mock's navy disc aesthetic and our dark scene are compatible). Key props: `paused` (pass true when the scene's tab/panel is not visible — it stops the frameloop), `topDown` for small static medallions, controlled `selectedBody` + `highlightBodies` for isolate interactions, `relationship={{a,b,nameA,nameB,blend,comp,...}}` mode for synastry/composite (see src/pages/RelationshipJourneyPage.js for a working example of every prop).
- Reuse existing data hooks/components/logic — never re-implement API calls that exist in src/Utilities/api.js or duplicate business logic from existing pages. When restyling an existing flow, extract/reuse its handlers.
- Real routes, not mock hrefs: signup → /signUp, dashboard → /dashboard/:userId, chart detail → /dashboard/:userId/chart/:chartId, relationship detail → /dashboard/:userId/relationship/:compositeId, celebrities → /celebrities, celebrity relationships → /celebrity-relationships, privacy/terms → /privacy-policy /terms-of-service.
- Every dynamic region must handle loading and absent-data states gracefully (existing pages show the patterns).
- `npm run build` must pass at the end — run it, fix what you broke. Report: files created/modified and any mock details you intentionally deviated from.
