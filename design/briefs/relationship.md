# Task: Ink relationship page — implement design/ink/relationship.html

Read design/briefs/_shared.md first and obey every constraint there.

Build src/pages/InkRelationshipPage.js + InkRelationshipPage.css implementing design/ink/relationship.html with REAL data. Route params /dashboard/:userId/relationship/:compositeId (useParams; do not wire routes). Data loading: copy the exact pattern from src/pages/RelationshipJourneyPage.js — getUserCompositeCharts + fetchRelationshipAnalysis + fetchUser for both partners (keeping birthCharts AND relationshipAppProfile.romanticProfileBlurb), getRelationshipCardSummary for the archetype (label/blurb/tier/score), cluster panels helper (support/challenge/synthesis per cluster), synastry aspect rows, composite chart data, and the scene adapters (toChartScenePlacements/toSynastrySceneAspects/toChartSceneAspects).

Page structure per the mock:
- InkNav variant="app" (activeSegment="relationships"); header "{A} & {B}" + meta "Synastry & composite reading · created {date}".
- Sticky roman-numeral tabs (i–v), client-side panels:
  i. **Overview** — partners strip: partner card (name, birth data, big three "☉ · ☽ · ↑" from their birth charts, plus their romanticProfileBlurb as an italic line when present) | the mock's inline knot SVG (copy it verbatim, gold+blue circles + ✦) | partner B card. Below: headline, gold archetype label (getRelationshipCardSummary cardHeadline), archetype blurb + overview narrative paragraphs, score-line "{overall}% overall connection · strongest in {top clusters}". Right column: navy medallion with the REAL bi-wheel — ChartScene relationship mode with blend=1, comp=0 (see RelationshipJourneyPage's sceneProps for the full relationship prop shape), caption "{A} outer, {B} inner", Caveat annotation.
  ii. **Synastry** — left ChartScene relationship medallion (blend=1) + color legend (partner A gold dot / partner B blue dot); right copy + the REAL cross-aspect list, tightest first (planet pair, aspect type, orb, harmonious/dynamic classing by aspect type) — clicking a row isolates that thread on the wheel via highlightA/highlightB (hover previews, click pins — the app's established grammar; see RelationshipJourneyPage's isoRow pattern).
  iii. **Composite** — mirrored two-column: composite narrative left (clusterPanels('composite') synthesis + composite Sun/Moon/Ascendant placements from compositeChart.planets), right ChartScene medallion in composite state (relationship mode with blend=1, comp=1), Caveat annotation "the chart of the 'we' ↑".
  iv. **360 Analysis** — pmenu of the five clusters (Harmony/Passion/Connection/Stability/Growth as the app names them — use the app's cluster keys, not the mock's), "N of 5" counter; article: title, animated score bar ({score}%), factor chips (top scored items per cluster — see RelationshipJourneyPage's pillar factors derivation), then Support Patterns / Growth Challenges / Synthesis sections from the cluster panels.
  v. **Ask Stellium** — left small relationship medallion + context card ("{A} & {B} · Synastry + Composite", "Strongest: {top two clusters with scores}"); right REAL Ask via src/UI/askStellium/AskStelliumPanel.js (contentType="relationship", contentId=compositeId, relationshipScoredItems — see RelationshipJourneyPage usage), restyled to the mock's chat look; footer "1 credit per question".

Pause any ChartScene whose panel is hidden. Handle loading/error/not-found. Do not modify RelationshipJourneyPage or other existing pages.
